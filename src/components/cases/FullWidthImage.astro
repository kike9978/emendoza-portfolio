---
import { Image } from "astro:assets";

const { imgSrc, alt } = Astro.props;
---

<Image
    class="col-start-1 md:col-span-12 md:mb-28 max-w-[100vw] md:max-w-[calc(100vw_-_1rem)] relative -left-4 md:-left-7"
    src={imgSrc}
    alt={alt}
/>
